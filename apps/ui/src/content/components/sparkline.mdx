---
title: 'Sparkline'
description: 'A chromeless trend line sized for a table cell, a list row or the trend slot of a c2-stat.'
category: 'Chart'
package: '@c2n/chart'
tag: 'c2-sparkline'
apiTags: ['c2-sparkline', 'c2-chart-series', 'c2-chart-legend', 'c2-chart-tooltip']
---

import UsageBlock from '../../components/UsageBlock.astro'
import CodeBlock from '../../components/CodeBlock.astro'
import ChartEnergyDemo from '../../components/ChartEnergyDemo.astro'

import { Sparkline } from '@c2n/chart/sparkline.js'
import { ChartSeries } from '@c2n/chart/chart-series.js'
import { ChartLegend } from '@c2n/chart/chart-legend.js'
import { ChartTooltip } from '@c2n/chart/chart-tooltip.js'
import { Button } from '@c2n/button'

<ChartEnergyDemo />

No axes, no grid, no legend, no tooltip and no card: the whole box is the data. `c2-sparkline` is an
`inline-flex` element 120×32px by default, which is the size a table cell, a list row or the `trend` slot of
a [`c2-stat`](/web-components/components/stat) actually has.

It takes the bare `number[]` shape as well as rows, which is usually all a cell has:

```html tag=CodeBlock
<c2-sparkline data="[3, 5, 4, 8, 6, 11]" tone="auto"></c2-sparkline>
```

uPlot is what makes a sparkline per row affordable — the engine loads once for the page, and each instance
is a single canvas.

## Installation

```bash tag=CodeBlock
npm install @c2n/chart uplot
```

## Usage

```html tag=UsageBlock,package=@c2n/chart
<style>
  .kpis {
    display: grid;
    grid-template-columns: auto auto;
    gap: 10px 16px;
    align-items: center;
    font: 14px system-ui;
  }
  .row {
    display: flex;
    align-items: center;
    gap: 12px;
    font: 14px system-ui;
  }
  .energy-linked,
  .energy-live {
    display: grid;
    width: 100%;
    gap: 10px;
  }
  .energy-expanded {
    --c2-chart--width: 100%;
    --c2-chart--height: 120px;
  }
  .energy-live__controls {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 12px;
    color: var(--site-color-on-surface-muted);
  }
</style>

<div data-label="Default">
  <div class="row">
    <c2-sparkline data="[437.43,403.2,398.36,412.94,399.01,353.83,379.36,294.73,338.2,380.45]" tone="auto"></c2-sparkline>
    <span>France nuclear generation, 2015–2024</span>
  </div>
</div>

<div data-label="Custom legend + tooltip" data-energy-custom>
  <div class="energy-linked">
    <c2-sparkline
      id="sparkline-energy-custom"
      class="energy-expanded"
      x-field="year"
      cursor="x"
      tooltip="axis"
      data='[{"year":2015,"nuclear":437.43},{"year":2016,"nuclear":403.2},{"year":2017,"nuclear":398.36},{"year":2018,"nuclear":412.94},{"year":2019,"nuclear":399.01},{"year":2020,"nuclear":353.83},{"year":2021,"nuclear":379.36},{"year":2022,"nuclear":294.73},{"year":2023,"nuclear":338.2},{"year":2024,"nuclear":380.45}]'
    >
      <c2-chart-series field="nuclear" label="Nuclear"></c2-chart-series>
    </c2-sparkline>
    <c2-chart-legend for="sparkline-energy-custom"></c2-chart-legend>
    <c2-chart-tooltip for="sparkline-energy-custom"></c2-chart-tooltip>
  </div>
</div>

<div data-label="Realtime replay" data-energy-realtime>
  <div class="energy-live">
    <c2-sparkline class="energy-expanded" x-field="year" max-points="180" data-energy-fields="nuclear">
      <c2-chart-series field="nuclear" label="Nuclear"></c2-chart-series>
    </c2-sparkline>
    <div class="energy-live__controls">
      <c2-button>Replay</c2-button>
      <span data-energy-status>Ready at 2018</span>
    </div>
  </div>
</div>
```

The values are a France 2015–2024 extract from the public [Our World in Data Energy dataset](https://github.com/owid/energy-data), measured in terawatt-hours. The same extract drives every chart page; the sparkline simply removes the axes and surrounding chrome.

## Tone

`tone="auto"` compares the last value with the first and paints the line with the positive or negative tone
— `--c2-chart__tone-positive--color` and `--c2-chart__tone-negative--color`. `positive` and `negative` force
one of them, and `neutral` (the default) keeps the series' palette colour, which is what you want when the
sparkline sits next to other charts that share it.

A flat series, a series with fewer than two points, or a non-numeric end falls back to the palette colour
rather than guessing a direction.

## Sizing

The size is the API: `--c2-chart--width` (120px) and `--c2-chart--height` (32px), both redefined on the host
rather than merely defaulted, so an author rule on the element still wins. Padding is `0` and the border and
background are removed, so the line reaches the edges of the box.

```css tag=CodeBlock
c2-table c2-sparkline {
  --c2-chart--width: 88px;
  --c2-chart--height: 24px;
  --c2-chart__line--width: 1px;
}
```

## What it leaves out, and putting it back

The axes, grid, cursor, legend and tooltip are switched off in the constructor rather than made impossible,
so a sparkline that does want a hover readout can set `tooltip="axis"` and `cursor="x"` back on. Series
children work too, when the row shape is what you have rather than a bare array.

## Theming

The stroke colour, the two tone colours and everything else come from the shared `--c2-chart__*` namespace,
so a sparkline in a table and a [line chart](/web-components/components/line-chart) above it agree on colour
without any extra configuration.
