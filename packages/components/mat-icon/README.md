# Mat Icon

Material Symbols ligature rendered through a standard custom element.

```html
<c2-mat-icon>home</c2-mat-icon>
```

The default slot is consumer-owned ligature text. Style the host with the documented font-size, font-weight, and color custom properties; no redundant CSS part is exposed.
