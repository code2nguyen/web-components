# Kbd

`@c2n/kbd` provides `c2-kbd`, a semantic and themeable keyboard-key label for shortcuts and command hints.

```html
<script type="module">
  import '@c2n/kbd'
</script>

Press <c2-kbd>Ctrl + K</c2-kbd> to search.
```
