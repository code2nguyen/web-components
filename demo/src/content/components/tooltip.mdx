---
    
    title: "Tooltip"
    slug: "tooltip"
---

import MdxCodeBlock from '../../components/MdxCodeBlock.astro'
import { Tooltip } from '@c2n/tooltip'

# Tooltip

```html tag=MdxCodeBlock
<div class="container">
  <button class="tooltip-button">
    Hover me !
    <c2-tooltip>This is tooltip</c2-tooltip>
  </button>
</div>
<style>
  .container {
    display: flex;
    gap: 16px;
    align-items: center;
  }
  .tooltip-button {
    position: relative;
  }
</style>
```

`c2-tooltip` allow users to get contextual help or information about specific components when hovering or focusing on them.
