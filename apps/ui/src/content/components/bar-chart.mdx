---
title: 'Bar chart'
description: 'Bars over categories or time buckets, with several series drawn side by side within each band.'
category: 'Chart'
package: '@c2n/chart'
tag: 'c2-bar-chart'
apiTags: ['c2-bar-chart', 'c2-chart-series', 'c2-chart-legend', 'c2-chart-tooltip']
---

import UsageBlock from '../../components/UsageBlock.astro'
import CodeBlock from '../../components/CodeBlock.astro'
import ChartEnergyDemo from '../../components/ChartEnergyDemo.astro'

import { BarChart } from '@c2n/chart/bar-chart.js'
import { ChartSeries } from '@c2n/chart/chart-series.js'
import { ChartLegend } from '@c2n/chart/chart-legend.js'
import { ChartTooltip } from '@c2n/chart/chart-tooltip.js'
import { Button } from '@c2n/button'

<ChartEnergyDemo />

Bars for comparing discrete things: teams, plans, weekdays, buckets. Several series are drawn side by side
within each band, and `bar-width` and `bar-gap` decide how much of the band the group takes and how much of
the group is air between the bars.

A bar chart reads its x as discrete slots rather than as a continuous scale, so the axis is padded by half a
band at each end and the cursor snaps to the band instead of landing between two points.

On first display the plot reveals from the zero baseline upward, so grouped bars grow together. Later data
updates stay immediate; set `animation="none"` when even the initial entrance should be disabled.

## Installation

```bash tag=CodeBlock
npm install @c2n/chart uplot
```

## Usage

```html tag=UsageBlock,package=@c2n/chart
<style>
  .plot {
    width: 100%;
    --c2-chart--height: 240px;
  }
  .energy-linked,
  .energy-live {
    display: grid;
    width: 100%;
    gap: 10px;
  }
  .energy-live__controls {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 12px;
    color: var(--site-color-on-surface-muted);
  }
</style>

<div data-label="Default">
  <c2-bar-chart
    class="plot"
    label-field="year"
    x-type="category"
    data='[{"year":2020,"hydro":62.52,"wind":40.05,"solar":13.09},{"year":2021,"hydro":59.54,"wind":37.12,"solar":15.28},{"year":2022,"hydro":45.56,"wind":38.2,"solar":19.34},{"year":2023,"hydro":55.9,"wind":50.48,"solar":22.59},{"year":2024,"hydro":71.12,"wind":45.43,"solar":24.87}]'
  >
    <c2-chart-series field="hydro" label="Hydro"></c2-chart-series>
    <c2-chart-series field="wind" label="Wind"></c2-chart-series>
    <c2-chart-series field="solar" label="Solar"></c2-chart-series>
  </c2-bar-chart>
</div>

<div data-label="Custom legend + tooltip" data-energy-custom>
  <div class="energy-linked">
    <c2-bar-chart
      id="bar-energy-custom"
      class="plot"
      label-field="year"
      x-type="category"
      data='[{"year":2020,"nuclear":353.83,"hydro":62.52,"wind":40.05,"solar":13.09},{"year":2021,"nuclear":379.36,"hydro":59.54,"wind":37.12,"solar":15.28},{"year":2022,"nuclear":294.73,"hydro":45.56,"wind":38.2,"solar":19.34},{"year":2023,"nuclear":338.2,"hydro":55.9,"wind":50.48,"solar":22.59},{"year":2024,"nuclear":380.45,"hydro":71.12,"wind":45.43,"solar":24.87}]'
    >
      <c2-chart-series field="nuclear" label="Nuclear"></c2-chart-series>
      <c2-chart-series field="hydro" label="Hydro"></c2-chart-series>
      <c2-chart-series field="wind" label="Wind"></c2-chart-series>
      <c2-chart-series field="solar" label="Solar"></c2-chart-series>
    </c2-bar-chart>
    <c2-chart-legend for="bar-energy-custom"></c2-chart-legend>
    <c2-chart-tooltip for="bar-energy-custom"></c2-chart-tooltip>
  </div>
</div>

<div data-label="Realtime replay" data-energy-realtime>
  <div class="energy-live">
    <c2-bar-chart class="plot" label-field="year" x-type="category" data-energy-fields="hydro,wind,solar">
      <c2-chart-series field="hydro" label="Hydro"></c2-chart-series>
      <c2-chart-series field="wind" label="Wind"></c2-chart-series>
      <c2-chart-series field="solar" label="Solar"></c2-chart-series>
    </c2-bar-chart>
    <div class="energy-live__controls">
      <c2-button>Replay</c2-button>
      <span data-energy-status>Ready at 2018</span>
    </div>
  </div>
</div>
```

The values come from one France 2015–2024 extract of the public [Our World in Data Energy dataset](https://github.com/owid/energy-data), in terawatt-hours. Here the same yearly rows used by the other chart pages are interpreted as categorical groups.

## Labelling the bands

`label-field` names the key that holds each band's label, and `x-type="category"` reads the x as slots. The
tick shows the label rather than the slot index, and the tooltip follows the band. Without a `label-field`
the bars fall back to `x-field` (or the row index), which is what a time-bucketed histogram usually wants.

## Bar geometry

`bar-width` is the share of the band a group occupies and `bar-gap` the share of that group taken by the
gaps between grouped bars, both from 0 to 1. The defaults — `0.6` and `0.1` — give comfortable bars with a
small gutter between the series of a group. Narrow the width for a sparse, editorial look; widen it to `1`
for a histogram with no air between bands.

```html tag=CodeBlock
<c2-bar-chart label-field="bucket" x-type="category" bar-width="1" bar-gap="0">
  <c2-chart-series field="count"></c2-chart-series>
</c2-bar-chart>
```

## Theming

Bars use the shared palette, `--c2-chart__series-1--color` through `--c2-chart__series-8--color`, wired by
`@c2n/theme` to the `--c2-theme--chart-series-*` tokens; a single series can override its slot with
`<c2-chart-series color="…">`. Every other `--c2-chart__*` variable — the grid, the axis, the tooltip, the
card around the plot — is shared with the rest of the package, so setting them once themes every chart.

Round the value end of each bar with `--c2-chart__bar--border-radius`. It accepts a unitless value from `0`
(square) to `0.5` (fully rounded) and keeps the end at the zero baseline square:

```css tag=CodeBlock
c2-bar-chart {
  --c2-chart__bar--border-radius: 0.18;
}
```
