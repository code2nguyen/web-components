# Masonry validation checkpoint (2026-09-25)

- `npm run build -w packages/components/masonry`, `npm run build:tools`, `npm run build -w apps/ui`, `npm run docs:check`, `npm run check:dogfood`, `npm run lint`, `npm run format:check`, and `npm run test:type-check` pass.
- `npm pack -w packages/components/masonry --dry-run --json` passes with a writable temporary npm cache; its 18 published entries include the manifest, both JavaScript entry points, type declarations, README, and React/Vue outputs.
- The masonry Playwright suite passes in Chromium, Firefox, and WebKit (137 passed, 16 intentionally skipped). It verifies valid snapshot restoration at all four ranges, default localStorage save/restore, opt-out and custom keys, explicit layout precedence, malformed/unavailable storage handling, authored reorder precedence, mutation cancellation, border-sensitive breakpoints, and a 100-tile repack. Playwright mouse input covers dragging in all three engines. Eight Chromium-only DevTools-protocol cases cover touch and pen move/resize commits and cancellations through browser-generated `pointerdown` events with `isTrusted === true`; Firefox/WebKit retain synthetic pointer-contract coverage, and the Chromium-only cases are skipped there. This is trusted browser simulation, not physical touch or pen hardware validation; no hardware pen device is attached to this workspace.
- `npm run ui:build` stops in the unrelated `apps/examples/orders-admin-angular:build` step with `SIGABRT`, before the Astro UI step. The same Angular build fails when run alone. The direct Astro UI build passes and emits the masonry docs, gallery, and API routes. This is not a masonry or UI compilation failure.
- The full-library `npm run check:style-contracts` exits 1 with 2,751 static findings. Of these, the two new masonry tags have 27 `missing-case` registry findings and no `unused-property` findings. Their 27 variables and six parts have separate focused browser target-effect tests in `masonry.a11y.spec.ts`. The other 2,724 findings (2,604 `missing-case`, 120 `unused-property`) are the pre-existing incomplete library audit tracked in [feature 004](../004-verify-css-contracts/audit-results.md). The full-library verifier is not claimed to pass.
